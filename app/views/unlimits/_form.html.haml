= simple_form_for @unlimit, html: { class: 'form-horizontal' } do |f|
  - f.with_options input_html: { class: 'col-md-6' } do |f|
    = f.input :price
    = f.input :name
  .form-actions
    = f.button :submit, class: 'btn-primary'
    = link_to t('cancel'), schedule_path, class: 'btn'
