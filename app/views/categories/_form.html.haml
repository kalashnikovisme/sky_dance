- @categories.each do |cat|
  = simple_form_for cat, :html => { :class => 'form-horizontal' } do |f|
    = f.input :describe
    %br
  = link_to update_categories_path, :class => 'btn btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), new_group_path, :class => 'btn'
