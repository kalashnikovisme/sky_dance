= simple_form_for @video, :html => { :class => 'form-horizontal' } do |f|
  - f.with_options :input_html => { :class => :span6 } do |f|
    = f.input :link
    .form-actions
      = f.button :submit, t('.add_video'), :class => 'btn btn-primary'
      = link_to t('cancel'), @genre, :class => 'btn'
