= simple_form_for @office_map, :html => { :class => 'form-horizontal' } do |f|
  = f.input :link
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), office_map_path, :class => 'btn'
