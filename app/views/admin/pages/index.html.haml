.row-fluid
  = link_to 'Новая страница', new_admin_page_path, class: 'btn btn-primary'
  %table.table.table-striped.table-bordered
    %thead
      %th Название
      %th Текст
      %th Slug
    %tbody
      - @pages.each do |page|
        %tr
          %td= link_to page.title, edit_admin_page_path(page)
          %td= strip_tags page.text.first(200)
          %td= page.slug
